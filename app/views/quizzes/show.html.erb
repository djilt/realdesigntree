        <div class="row" >
			<div class="col-md-12 col-lg-12 col-sm-12 prezentacja "> 
<p id="notice"><%= notice %></p>

<p>
  <strong>Content:</strong>
  <%= @quiz.content %>
  <% @quiz.questions.each do |question| %>
	<div id="answer-<%= question.id %>" class="question">
	  <h2><%= question.content %></h2>
  	  <% question.answers.each do |answer| %> 
           <div class="col-lg-2 answer"><span><%= answer.content %></span></div>
  	  <% end %>
	</div>
  <% end %>
</p>
<%= link_to 'Back', quizzes_path %>
</div>
</div>